services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file: .env
    depends_on:
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_started
    volumes:
      - ./backend:/app
      - uploads_data:/app/uploads
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
    depends_on:
      - backend
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: imocha
      POSTGRES_PASSWORD: imochapassword
      POSTGRES_DB: imocha_hub
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U imocha -d imocha_hub"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
      - "6334:6334"
    # NOTE: For production, add QDRANT__SERVICE__API_KEY via docker-compose.prod.yml
    # or set it in the environment directly. Do NOT set it here — an empty value
    # enables Qdrant auth with no key, blocking all requests.
    volumes:
      - qdrant_data:/qdrant/storage
    restart: unless-stopped

volumes:
  pg_data:
  qdrant_data:
  uploads_data:
